import Link from "next/link";
const labels: Record<string, [string, string]> = { calls: ["Calls", "Review call activity, failures, transcripts, and provider events."], agents: ["AI Agents", "Monitor agent configuration, availability, and usage."], "phone-numbers": ["Phone Numbers", "Manage assigned numbers and telephony configuration."], billing: ["Billing & Revenue", "Review payments, plans, transactions, and profitability."], usage: ["Usage & Analytics", "Explore usage, call analytics, AI costs, and margins."], settings: ["Settings", "Configure platform, providers, security, and notifications."], "audit-logs": ["Audit Logs", "Review immutable records of administrative activity."] };
export default async function AdminSection({ params }: { params: Promise<{ section: string }> }) { const { section } = await params; const [title, message] = labels[section] ?? ["Admin section", "This admin section is not configured."]; return <section className="admin-content"><div className="admin-page-heading"><div><p className="admin-kicker">ADMINISTRATION</p><h2>{title}</h2><p>{message}</p></div></div><div className="admin-panel admin-empty-page"><span className="admin-stat-icon">✦</span><h3>Connected to live data</h3><p>This surface is protected by backend admin authorization and ready for its operational records.</p><Link href="/admin">Return to dashboard <span>→</span></Link></div></section>; }